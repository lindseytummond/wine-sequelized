<h1>Wines of the World</h1>

<h2>Wines you have Tried:</h2>

<ul>
  {{#each wines}}
    {{#unless drank}}
      {{> wines/wine-block drank=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Wine you would like to Try:</h2>

<ul>
  {{#each wines}}
    {{#if drank}}
      {{> wines/wine-block drank=false}}
    {{/if}}
  {{/each}}
</ul>


<h2>Add a Wine</h2>
<form class="create-form">

    <div class="form-group">
        <label for="ca">Wine Name:</label>
        <input type="text" id="ca" name="name">
    </div>

    <button type="submit">Add a Wine</button>

</form>