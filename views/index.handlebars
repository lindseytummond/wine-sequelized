<h1>Wine</h1>

<h2>Wines I have Tried:</h2>

<ul>
  {{#each wines}}
    {{#unless tasty}}
      {{!-- {{> wines/wine-block taste=true}} --}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Wines I want to Try:</h2>

<ul>
  {{#each wines}}
    {{#if tasty}}
      {{!-- {{> wines/wine-block taste=false}} --}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Wine</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Wine Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  {{!-- <div class="form-group">
    <label for="tasty">Wine?</label><br>
    <input type="radio" name="tasty" value="1" checked> No, Thanks!<br>
    <input type="radio" name="tasty" value="0"> Yes, Please!
  </div> --}}

  <button type="submit">Add Wine</button>
</form>