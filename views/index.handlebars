<h1>Wine Tasting</h1>

<h2>Wines I want to Try:</h2>

<ul>
  {{#each wines}}
    {{#unless taste}}
      {{> wines/wine-block taste=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Wines I have Tried:</h2>

<ul>
  {{#each wines}}
    {{#if taste}}
      {{> wines/wine-block taste=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Wine</h2>
<form class="create-form">

  <div class="form-group">
    <label for="wine">Wine Name:</label>
    <input type="text" id="wine" name="name">
  </div>

  {{!-- <div class="form-group">
    <label for="taste">Wine?</label><br>
    <input type="radio" name="taste" value="1" checked> No, Thanks!<br>
    <input type="radio" name="taste" value="0"> Yes, Please!
  </div> --}}

  <button type="submit">Add Wine</button>
</form>